<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orient.ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.sw.MultiSwitchActivity">

        </activity>
        <activity android:name=".ui.activity.table.TableActivity" />
        <activity android:name=".ui.activity.placeholder.PlaceHolderActivity" />
        <activity android:name=".ui.activity.timeline.TimelineActivity" />
        <activity android:name=".ui.activity.gridpage.GridPageActivity" />
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.doubleside.DoubleSideLayoutActivity" />
    </application>

</manifest>